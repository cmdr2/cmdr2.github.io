---
title: "Post from Jul 16, 2026"
date: 2026-07-16T10:23:51
slug: "1784197431"
tags:
  - puppetry
  - worklog
---

Released two new features in Puppetry (in Early Access):
- a "Presets" plugin for Puppetry
- the ability to specify different "motion smoothness" settings for controller movement and rotation

The "Presets" plugin allows a user to store and use "preset" values for attachment and button settings in Puppetry. In complex rigs, each character may often have a complicated set of settings in Puppetry (shapekeys, custom response curves etc). Switching between characters in Puppetry involved recreating all of those settings each time during recording.

With the "Presets" feature, a user can define two presets (one for each character), and set up different attachments and button actions for each character. The user can then easily switch between the presets to animate the two characters (even during recording, using a button), without having to reconfigure each character.

It is currently implemented as a "custom module" (that can be specified in the `Advanced Settings` panel in Puppetry). But this will be included in the main project shortly.